export interface Project {
  id: number;
  title: string;
  description: string;
  imageUrl: string;
  tags: string[];
}

export interface ProcessItem {
  id: number;
  title: string;
  description: string;
  imageUrl: string;
}