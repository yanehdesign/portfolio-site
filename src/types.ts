export interface Project {
  id: number;
  title: string;
  description: string;
  imageUrl: string;
}

export interface ProcessItem {
  id: number;
  title: string;
  description: string;
  imageUrl: string;
}